name: Deploy Docs

on:
  push:
    branches:
      - main
    paths:
      - 'website/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

# Configurar permisos para GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Permitir solo un deploy concurrente
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependencias
        working-directory: ./website
        run: npm install

      - name: Build Docusaurus
        working-directory: ./website
        run: npm run build

      - name: Upload artifact para GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./website/build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # Job alternativo si no usas GitHub Pages
  # Descomenta esto y comenta el job 'deploy' anterior si prefieres otro método
  # deploy-alternative:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: false  # Cambiar a true para activar
  #   
  #   steps:
  #     - name: Deploy a servidor custom
  #       run: |
  #         echo "Aquí irían los comandos para deploy a tu servidor"
  #         echo "Por ejemplo: rsync, scp, ftp, etc."
